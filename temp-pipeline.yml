parameters:
  - name: testParam
    type: string
    default: 'N/A'
trigger:
- main
# pool:
#   vmImage: 'ubuntu-latest'
pool: AZP-Pool
extends:
  template: -template.yaml
  parameters:
    testParam: ${{ parameters.testParam }}
