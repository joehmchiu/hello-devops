parameters:
  - name: testParam
    type: string
    default: 'N/A'
trigger:
- main
# pool:
#   vmImage: 'ubuntu-latest'
pool: AZP-Pool
extends:
  template: hello-template.yaml
  parameters:
    testParam: ${{ parameters.testParam }}
