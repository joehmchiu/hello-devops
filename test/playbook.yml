- name: ansible test for devops
  hosts: macfoo
  tasks:
  - name: hello world
    debug:
      msg: "Hello world!"
  - name: output to log
    shell: echo "Hello world - $(date +'%T %F')" >> /tmp/foo.log

